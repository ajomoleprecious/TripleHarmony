1. git add .

2. git commit -m

3. git pull - check of er conflicten zijn

(indien geen conflict)

4.1 git push

(indien wel conflict)

4.2 los op in editor (keuzes: accepteer incoming of curent of both)

4.3 Ga terug naar stap 1.

5. git push


Een branch aan maken en erop werken

1. git checkout -b "branch naam"

2. check links beneden of je idd in een nieuwe branch zit

indien wel 

3. Blijf werken en dan git add, git commit en git push --set-upstream origin "branch naam"